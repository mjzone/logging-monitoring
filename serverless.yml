service: logging-monitoring

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-1

functions:
  generate:
    handler: handler.generate
    events:
      - http:
          path: generate/{number}
          method: get
